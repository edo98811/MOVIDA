% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/7plot_heatmap.R
\name{plot_heatmap_movida}
\alias{plot_heatmap_movida}
\title{Plot Heatmap for MOVIDA Signatures}
\usage{
plot_heatmap_movida(
  se,
  features,
  row_column = NULL,
  pathway = NULL,
  use_symbol = FALSE,
  cluster_rows = TRUE,
  cluster_columns = FALSE,
  center_mean = TRUE,
  scale_row = FALSE,
  winsorize_threshold = NULL,
  plot_title = NULL,
  export_data = FALSE,
  ...
)
}
\arguments{
\item{se}{A \code{SummarizedExperiment} object containing the expression data.}

\item{features}{A character vector of feature names (e.g., gene names or IDs) to include in the heatmap.}

\item{row_column}{Optional. Name of the column in \code{rowData(se)} to use for matching features (e.g., gene symbols). If \code{NULL}, rownames of \code{se} are used.}

\item{pathway}{Optional. Name of the pathway or signature for labeling the plot.}

\item{use_symbol}{Logical. Deprecated. If \code{TRUE}, attempts to use gene symbols for row labels (currently not used).}

\item{cluster_rows}{Logical. If \code{TRUE}, cluster the rows (features) in the heatmap.}

\item{cluster_columns}{Logical. If \code{TRUE}, cluster the columns (samples) in the heatmap.}

\item{center_mean}{Logical. If \code{TRUE}, center each row by subtracting its mean.}

\item{scale_row}{Logical. If \code{TRUE}, scale each row to have zero mean and unit variance (z-score).}

\item{winsorize_threshold}{Optional. Numeric value. If provided, expression values are limited to the range \code{[-winsorize_threshold, winsorize_threshold]}.}

\item{plot_title}{Optional. Custom title for the heatmap plot.}

\item{export_data}{Logical. If \code{TRUE}, returns the processed heatmap data matrix instead of plotting.}

\item{...}{Additional arguments passed to the underlying \code{heatmap} function.}
}
\value{
If \code{export_data = TRUE}, returns a matrix of processed expression values. Otherwise, generates a heatmap plot and returns \code{NULL}.
}
\description{
Generates a heatmap for selected features (e.g., genes) from a SummarizedExperiment object, with options for centering, scaling, winsorizing, clustering, and exporting the processed data.
}
\details{
The function filters out features with zero variance and optionally centers, scales, and winsorizes the data. It supports both base R and ComplexHeatmap plotting (the latter is commented out). If no data is available after filtering, a warning is issued and \code{NULL} is returned.
}
\examples{
\dontrun{
plot_heatmap_movida(se, features = c("GeneA", "GeneB", "GeneC"))
}

}
